# Backend

Node.js, Express, TypeScript, and Prisma backend for the Expense Tracker.

Run `npm install` followed by `npm run dev` from this directory. PostgreSQL is configured through `DATABASE_URL` when database work is added.
